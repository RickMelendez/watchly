# Frontend Environment Variables
# Set these as Build Variables in Railway's "Variables" tab for the frontend service
# (They must be set BEFORE deployment since they are baked in at build time)

# Required: Set this to your Railway backend service's public URL
# (Find it in Railway → backend service → Settings → Public Networking)
REACT_APP_API_BASE_URL=https://your-backend.up.railway.app
